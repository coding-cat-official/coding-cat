import { Button, Input } from "@mui/joy";

export default function CategoryPasswordForm() {
  return (
  <>
    <Input placeholder="Enter Category Password" />
    <Button color="success"> Save </Button>
  </>
  );
  
}